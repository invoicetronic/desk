services:
  desk:
    image: invoicetronic/desk
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./desk.yml:/app/desk.yml
      - ./logs:/app/logs       # daily rolling log files
      - ./custom:/app/custom
