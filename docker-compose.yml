services:
  desk:
    image: invoicetronic/desk
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./desk.yml:/app/desk.yml
      - ./data:/app/data       # SQLite DB persisted on host (mkdir before first `up`)
      - ./logs:/app/logs       # daily rolling log files (mkdir before first `up`)
      - ./custom:/app/custom
