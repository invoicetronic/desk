name: Tests

on:
  push:
    branches-ignore:
      - test
  pull_request:
  workflow_call:

jobs:
  unit:
    name: Unit & Integration
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore tests/Unit/

      - name: Build
        run: dotnet build tests/Unit/ --no-restore

      - name: Test
        run: dotnet test tests/Unit/ --no-build

  e2e:
    name: E2E (Playwright)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore tests/E2E/

      - name: Build
        run: dotnet build tests/E2E/ --no-restore

      - name: Install Playwright browsers
        run: pwsh tests/E2E/bin/Debug/net10.0/playwright.ps1 install --with-deps chromium

      - name: Test
        run: dotnet test tests/E2E/ --no-build
